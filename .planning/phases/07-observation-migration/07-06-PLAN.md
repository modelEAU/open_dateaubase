---
phase: 07-observation-migration
plan: 06
type: execute
domain: schema-migration
---

<objective>
Update all read (get_*) functions in value_repository.py to join through the
Observation table instead of querying Channel_ID and Timestamp directly from the
payload tables. Also write the v2.2.0 clean baseline CREATE script.

Purpose: Completes the API layer update. After this plan, the full read path
(timeseries queries, thumbnail lookups, image metadata, get_values_for_metadata)
all route through Observation.

Output:
- Updated `api/v1/repositories/value_repository.py` (read path)
- New `sql_generation_scripts/v2.2.0_create_mssql.sql`
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@api/v1/repositories/value_repository.py
@sql_generation_scripts/v2.1.0_create_mssql.sql

**Functions to update in value_repository.py (read path):**

get_scalar_values(conn, channel_id, from_dt, to_dt, operational_only):
  Current: SELECT v.[Timestamp], v.[Value] FROM [dbo].[Value] v WHERE v.[Channel_ID] = ?
  New: Join via Observation — o.[Timestamp] and o.[Channel_ID] replace v.[Timestamp]/v.[Channel_ID]
  operational_only subquery: also re-routes through Observation (see context below)

get_vector_values(conn, channel_id, from_dt, to_dt):
  Current: WHERE vv.[Channel_ID] = ? → New: JOIN Observation o ON ... WHERE o.[Channel_ID] = ?

get_matrix_values(conn, channel_id, from_dt, to_dt):
  Same pattern as vector.

get_image_values(conn, channel_id, from_dt, to_dt):
  Current: WHERE vi.[Channel_ID] = ? → New: JOIN Observation o, WHERE o.[Channel_ID] = ?

get_image_thumbnail(conn, channel_id, timestamp):
  Current: WHERE [Channel_ID] = ? AND [Timestamp] = ? → New: JOIN Observation

get_image_metadata_by_timestamp(conn, channel_id, timestamp):
  Same pattern as thumbnail.

get_values_for_metadata(conn, channel_id, value_type_id, ...):
  Dispatcher function — signature unchanged, dispatches to updated get_* functions.

**operational_only subquery rewrite:**
Current (nested subqueries reference v.[Channel_ID] and v.[Timestamp] directly):
  AND (NOT EXISTS (
      SELECT 1 FROM dbo.Channel statusC
      JOIN dbo.Value sv ON sv.Channel_ID = statusC.Channel_ID
      WHERE statusC.StatusChannel_ID = v.Channel_ID
        AND sv.Timestamp <= v.Timestamp
  ) OR EXISTS (
      SELECT 1 FROM dbo.Channel statusC
      JOIN dbo.Value sv ON sv.Channel_ID = statusC.Channel_ID
      JOIN dbo.QualityCode qc ON qc.QualityCode_ID = CAST(sv.Value AS INT)
      WHERE statusC.StatusChannel_ID = v.Channel_ID
        AND sv.Timestamp <= v.Timestamp
        AND qc.IsUsable = 1
  ))

New (v has no Channel_ID/Timestamp; use outer alias o for the main observation):
  AND (NOT EXISTS (
      SELECT 1 FROM dbo.Channel statusC
      JOIN dbo.Observation so ON so.[Channel_ID] = statusC.[Channel_ID]
      JOIN dbo.Value sv ON sv.[Observation_ID] = so.[Observation_ID]
      WHERE statusC.[StatusChannel_ID] = o.[Channel_ID]
        AND so.[Timestamp] <= o.[Timestamp]
  ) OR EXISTS (
      SELECT 1 FROM dbo.Channel statusC
      JOIN dbo.Observation so ON so.[Channel_ID] = statusC.[Channel_ID]
      JOIN dbo.Value sv ON sv.[Observation_ID] = so.[Observation_ID]
      JOIN dbo.QualityCode qc ON qc.[QualityCode_ID] = CAST(sv.[Value] AS INT)
      WHERE statusC.[StatusChannel_ID] = o.[Channel_ID]
        AND so.[Timestamp] <= o.[Timestamp]
        AND qc.[IsUsable] = 1
  ))

Note: outer aliases in get_scalar_values after refactor are:
  v = Value row, o = Observation row (joined from v)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update all get_* read functions in value_repository.py</name>
  <files>api/v1/repositories/value_repository.py</files>
  <action>
Read the full file. Update each function:

1. get_scalar_values: change base query to:
   SELECT o.[Timestamp], v.[Value]
   FROM [dbo].[Value] v
   JOIN [dbo].[Observation] o ON o.[Observation_ID] = v.[Observation_ID]
   {where}
   ORDER BY o.[Timestamp]

   where clause starts as: WHERE o.[Channel_ID] = ?
   from_dt filter: AND o.[Timestamp] >= ?
   to_dt filter:   AND o.[Timestamp] <= ?

   operational_only appends the rewritten subquery from context above
   (uses aliases so = Observation, sv = status Value — outer main aliases are v, o).

   Return dict: {"timestamp": row[0], "value": row[1], "quality_code": None} — unchanged.

2. get_vector_values: change base query to:
   SELECT o.[Timestamp], vb.[BinIndex], vb.[LowerBound], vb.[UpperBound],
          vv.[Value], vv.[QualityCode]
   FROM [dbo].[ValueVector] vv
   JOIN [dbo].[Observation] o  ON o.[Observation_ID]  = vv.[Observation_ID]
   JOIN [dbo].[ValueBin]    vb ON vb.[ValueBin_ID]    = vv.[ValueBin_ID]
   {where}
   ORDER BY o.[Timestamp], vb.[BinIndex]

   where clause: WHERE o.[Channel_ID] = ?  (replace vv.[Channel_ID])
   from_dt/to_dt: AND o.[Timestamp] >= ? / <= ?

3. get_matrix_values: change base query to:
   SELECT o.[Timestamp], rb.[BinIndex] AS RowBinIndex, cb.[BinIndex] AS ColBinIndex,
          vm.[Value], vm.[QualityCode]
   FROM [dbo].[ValueMatrix] vm
   JOIN [dbo].[Observation] o  ON o.[Observation_ID]   = vm.[Observation_ID]
   JOIN [dbo].[ValueBin]    rb ON rb.[ValueBin_ID]     = vm.[RowValueBin_ID]
   JOIN [dbo].[ValueBin]    cb ON cb.[ValueBin_ID]     = vm.[ColValueBin_ID]
   {where}
   ORDER BY o.[Timestamp], rb.[BinIndex], cb.[BinIndex]

   where clause: WHERE o.[Channel_ID] = ?

4. get_image_values: change base query to:
   SELECT o.[Timestamp], vi.[ImageWidth], vi.[ImageHeight], vi.[NumberOfChannels],
          vi.[ImageFormat], vi.[FileSizeBytes], vi.[StorageBackend],
          vi.[StoragePath], vi.[QualityCode]
   FROM [dbo].[ValueImage] vi
   JOIN [dbo].[Observation] o ON o.[Observation_ID] = vi.[Observation_ID]
   {where}
   ORDER BY o.[Timestamp]

   where clause: WHERE o.[Channel_ID] = ?

5. get_image_thumbnail: change query to:
   SELECT vi.[Thumbnail]
   FROM [dbo].[ValueImage] vi
   JOIN [dbo].[Observation] o ON o.[Observation_ID] = vi.[Observation_ID]
   WHERE o.[Channel_ID] = ? AND o.[Timestamp] = ?

6. get_image_metadata_by_timestamp: change query to:
   SELECT vi.[StoragePath], vi.[ImageFormat]
   FROM [dbo].[ValueImage] vi
   JOIN [dbo].[Observation] o ON o.[Observation_ID] = vi.[Observation_ID]
   WHERE o.[Channel_ID] = ? AND o.[Timestamp] = ?

   Return dict: {"storage_path": row[0], "image_format": row[1]} — unchanged.

7. get_values_for_metadata: dispatcher — no SQL changes, just verify it still
   delegates correctly to the updated functions above.
  </action>
  <verify>
- No function references v.[Channel_ID], vv.[Channel_ID], vm.[Channel_ID], or vi.[Channel_ID].
- No function references v.[Timestamp], vv.[Timestamp], vm.[Timestamp], or vi.[Timestamp].
- All WHERE clauses reference o.[Channel_ID] and o.[Timestamp].
- operational_only subquery aliases: so (status Observation), sv (status Value), o (outer Observation).
- Return dicts are structurally identical to pre-migration (same keys, same row indices).
  </verify>
  <done>All 6 get_* functions updated. Read path fully routes through Observation.</done>
</task>

<task type="auto">
  <name>Task 2: Write v2.2.0 clean baseline CREATE script</name>
  <files>sql_generation_scripts/v2.2.0_create_mssql.sql</files>
  <action>
Read sql_generation_scripts/v2.1.0_create_mssql.sql in full to use as the base.
Create v2.2.0_create_mssql.sql as a clean "fresh install" script — no ALTERs,
no transactions needed (this is for blank DB initialization only).

Changes from v2.1.0 baseline:
1. ADD CREATE TABLE [dbo].[Observation] (with PK, UQ, FK, CHECK as in 07-01)
   after the Channel table definition (Observation depends on Channel).

2. ADD CREATE INDEX [IX_Observation_Channel_Timestamp] on Observation.

3. REPLACE CREATE TABLE [dbo].[Value] with:
   CREATE TABLE [dbo].[Value] (
       [Observation_ID] BIGINT NOT NULL,
       [Value]          FLOAT,
       CONSTRAINT [PK_Value] PRIMARY KEY ([Observation_ID]),
       CONSTRAINT [FK_Value_Observation]
           FOREIGN KEY ([Observation_ID]) REFERENCES [dbo].[Observation] ([Observation_ID])
   );

4. REPLACE CREATE TABLE [dbo].[ValueVector] with:
   (Observation_ID BIGINT NOT NULL, ValueBin_ID INT NOT NULL, Value FLOAT, QualityCode INT)
   PK (Observation_ID, ValueBin_ID), FK to Observation, keep FK to ValueBin.

5. REPLACE CREATE TABLE [dbo].[ValueMatrix] with:
   (Observation_ID BIGINT NOT NULL, RowValueBin_ID INT NOT NULL, ColValueBin_ID INT NOT NULL,
    Value FLOAT, QualityCode INT)
   PK (Observation_ID, RowValueBin_ID, ColValueBin_ID), FKs to Observation + both ValueBins.

6. REPLACE CREATE TABLE [dbo].[ValueImage] with:
   (Observation_ID BIGINT NOT NULL as PK + FK→Observation, then all image columns)
   Remove ValueImage_ID IDENTITY.

7. In CREATE TABLE [dbo].[Annotation]: ADD [Observation_ID] BIGINT NULL +
   CONSTRAINT [FK_Annotation_Observation] FK to Observation.

8. REPLACE vw_ChannelStatus and vw_DeviceStatus with the updated versions (join via Observation).

9. Update the header comment: "-- Baseline CREATE script for schema v2.2.0"
   Update generated date.

Do NOT include INSERT seed data statements that are already in v2.1.0 — copy them verbatim.
  </action>
  <verify>
- File header says v2.2.0.
- Observation table is defined before Value, ValueVector, ValueMatrix, ValueImage.
- Value table has no Value_ID, Channel_ID, or Timestamp columns.
- ValueImage table has no ValueImage_ID column.
- Annotation table includes Observation_ID nullable column.
- Views use the Observation join pattern.
  </verify>
  <done>v2.2.0_create_mssql.sql exists as a clean fresh-install baseline. Read path and DDL baseline both complete.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] value_repository.py: grep for "v.\[Channel_ID\]" or "vv.\[Channel_ID\]" etc. — should return nothing
- [ ] value_repository.py: all WHERE clauses use o.[Channel_ID] and o.[Timestamp]
- [ ] operational_only subquery uses so/sv aliases, not the outer v/o aliases
- [ ] sql_generation_scripts/v2.2.0_create_mssql.sql exists
- [ ] Observation table definition appears before payload tables in the baseline script
- [ ] uv run pytest (unit/contract tests pass)
</verification>

<success_criteria>
- Complete read + write path in value_repository.py routes through Observation
- Clean v2.2.0 baseline CREATE script ready for fresh DB initialization
- All public function signatures are unchanged (no breaking API changes)
</success_criteria>

<output>
After completion, create .planning/phases/07-observation-migration/07-06-SUMMARY.md:

# Phase 07 Plan 06: API Query Path + v2.2.0 Baseline Script — Summary

**[One-liner]**

## Accomplishments
## Files Created/Modified
## Decisions Made
## Issues Encountered
## Next Step
Ready for 07-07-PLAN.md
</output>
