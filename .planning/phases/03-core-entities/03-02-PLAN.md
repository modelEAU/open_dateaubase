---
phase: 03-core-entities
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/pages/4_Channels.py
  - app/pages/5_Annotations.py
  - app/Home.py
autonomous: false
requirements:
  - CORE-03
  - CORE-04
must_haves:
  truths:
    - "User can view channels list with filter dropdowns"
    - "User can filter channels by equipment, parameter, or processing degree"
    - "User can create channel selecting FKs from dropdowns"
    - "User can edit existing channels"
    - "User can delete channels"
    - "User can view annotations list"
    - "User can create annotation with channel dropdown"
    - "User can edit and delete annotations"
  artifacts:
    - path: "app/pages/4_Channels.py"
      provides: "Channels CRUD page with filtering"
      min_lines: 150
    - path: "app/pages/5_Annotations.py"
      provides: "Annotations CRUD page"
      min_lines: 150
    - path: "app/Home.py"
      provides: "Updated navigation"
      contains: "4_Channels", "5_Annotations"
  key_links:
    - from: "app/pages/4_Channels.py"
      to: "app.api_client"
      via: "import"
      pattern: "from app.api_client import"
    - from: "app/pages/4_Channels.py"
      to: "app.components.form_dialog"
      via: "import"
      pattern: "from app.components.form_dialog import"
user_setup: []
---

<objective>
Create Streamlit CRUD pages for Channels and Annotations using form-based editing with FK dropdowns populated from API lookups.

Purpose: Provide complete CRUD UI for core entities with foreign key relationships.
Output: Two new Streamlit pages (Channels, Annotations) with form-based CRUD following Phase 02 patterns.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crud-reference/02-04-SUMMARY.md
@app/pages/2_Equipment.py
@app/pages/3_Campaigns.py
@app/components/form_dialog.py
@app/components/crud_form.py
@app/api_client.py
@app/Home.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Channels page with filtering</name>
  <files>
    app/pages/4_Channels.py
  </files>
  <action>
    Create app/pages/4_Channels.py following the pattern from app/pages/2_Equipment.py:

    Structure:
    1. Bootstrap imports and sys.path setup (copy from Equipment.py)
    2. Import api_client functions: list_channels, create_channel, update_channel, delete_channel, list_equipment_lookup, list_parameters_lookup, list_processing_degrees_lookup
    3. Import form_dialog components
    4. require_auth() and sidebar with user info
    5. st.title("Channels")

    Filter section (above the table):
    - Use st.columns(4) for filter dropdowns
    - Equipment filter: st.selectbox("Equipment", options=["All"] + equipment_options, key="filter_equipment")
    - Parameter filter: st.selectbox("Parameter", options=["All"] + parameter_options, key="filter_parameter")
    - Processing Degree filter: st.selectbox("Processing Degree", options=["All"] + degree_options, key="filter_degree")
    - "Apply Filters" button to reload data with selected filters

    Table display:
    - Load channels using list_channels() with filter params
    - Display with st.dataframe(use_container_width=True, on_select="rerun", selection_mode="single-row")
    - Store selected channel_id in session_state

    Action buttons (New, Edit, Delete) following Equipment.py pattern with form_dialog.

    Create form fields:
    - parameter_id: select (from lookup)
    - equipment_id: select (from lookup)
    - data_provenance_id: number input (nullable int)
    - processing_degree_id: select (from lookup)
    - value_type_id: number input (nullable int)

    Handle API errors with st.error() and return success boolean from handlers.
  </action>
  <verify>
    <automated>uv run python -c "import app.pages.4_Channels; print('Channels page imports OK')"</automated>
  </verify>
  <done>Channels page created with filtering and CRUD handlers</done>
</task>

<task type="auto">
  <name>Task 2: Create Annotations page</name>
  <files>
    app/pages/5_Annotations.py
  </files>
  <action>
    Create app/pages/5_Annotations.py following the pattern from Equipment.py:

    Structure:
    1. Bootstrap imports and sys.path setup
    2. Import api_client functions: list_annotations, create_annotation, update_annotation, delete_annotation, list_channels (for lookup - limited to first 100)
    3. Import form_dialog components
    4. require_auth() and sidebar
    5. st.title("Annotations")

    Table display:
    - Load annotations using list_annotations()
    - Display with st.dataframe (show: annotation_id, channel_id, title, start_time, end_time, author name)
    - Single-row selection

    Action buttons (New, Edit, Delete) with form_dialog.

    Create/Edit form fields:
    - channel_id: select (from channels lookup) - REQUIRED
    - annotation_type: select (from annotation_types lookup) or text input
    - start_time: datetime input (required)
    - end_time: datetime input (optional)
    - title: text input
    - comment: text area
    - campaign_id: number input (optional)
    - equipment_event_id: number input (optional)
    - author_person_id: number input (optional, show as disabled with note about auth)

    Note: Annotations API uses AnnotationCreate/Update schemas with specific validation (end_time >= start_time).

    Handle API errors and success messages following established patterns.
  </action>
  <verify>
    <automated>uv run python -c "import app.pages.5_Annotations; print('Annotations page imports OK')"</automated>
  </verify>
  <done>Annotations page created with CRUD handlers</done>
</task>

<task type="auto">
  <name>Task 3: Update Home.py navigation</name>
  <files>
    app/Home.py
  </files>
  <action>
    Update app/Home.py to include navigation links for the new pages:

    Add to the pages list:
    - "4_Channels" -> "Channels"
    - "5_Annotations" -> "Annotations"

    Follow the existing pattern from Home.py where it uses st.page_link() or the native Streamlit multipage navigation.

    If Home.py has manual navigation links, update those. If it uses native Streamlit multipage (pages/ directory), the pages will be auto-discovered based on filename numbering.
  </action>
  <verify>
    <automated>uv run python -c "import app.Home; print('Home page OK')"</automated>
  </verify>
  <done>Home navigation updated</done>
</task>

</tasks>

<verification>
- Channels page loads with filter dropdowns
- Selecting filters and clicking "Apply" reloads table with filtered data
- "New" button opens create dialog with FK dropdowns
- "Edit" button opens edit dialog pre-populated with selected row
- "Delete" button removes selected channel
- Annotations page loads with list
- Annotations can be created, edited, deleted
- Both pages handle API errors gracefully
- Navigation shows all 5 pages (Sites, Equipment, Campaigns, Channels, Annotations)
</verification>

<success_criteria>
- Channels page functional with filtering and CRUD
- Annotations page functional with CRUD
- Navigation updated
- Form dialogs work with FK dropdowns
- User can complete full CRUD workflows
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-entities/03-02-SUMMARY.md`
</output>
