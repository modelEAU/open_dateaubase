---
phase: 04-business-forms
plan: 08
type: execute
domain: streamlit
---

<objective>
Extend the sensor ingest page (9_Sensor_Ingest.py) with Vector and Matrix tabs,
enabling students to upload spectral data (one spectrum per timestamp) or 2D
distribution data (one matrix per timestamp) using the pre-defined axes.

Purpose: Once a UV spectrophotometer or particle counter axis is defined in plan 04-04,
students can submit full array-valued datasets. The page guides them through axis
selection, CSV format, and submission to /ingest/sensor-vector or /ingest/sensor-matrix.
Output: 9_Sensor_Ingest.py enhanced with three tabs (Scalar / Vector / Matrix).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-business-forms/04-05-SUMMARY.md
@.planning/phases/04-business-forms/04-07-SUMMARY.md
@app/pages/9_Sensor_Ingest.py
@app/api_client.py

**Tech stack available:** Streamlit (st.tabs), Pandas, Python csv module
**API contracts (from 04-07):**
  VectorSensorIngestRequest:
    equipment_id, parameter_id, unit_id, binning_axis_id,
    data_provenance_id=1, processing_degree_id=1
    observations: list[{timestamp, bin_values: list[float|None], quality_code}]

  MatrixSensorIngestRequest:
    equipment_id, parameter_id, unit_id, row_axis_id, col_axis_id,
    data_provenance_id=1, processing_degree_id=1
    observations: list[{timestamp, matrix: list[list[float|None]], quality_code}]
**Constraining decisions:**
- 9_Sensor_Ingest.py already exists — EXTEND it, do not rewrite
- Add list_binning_axes_lookup to imports
- Wrap existing content in tab: st.tabs(["Scalar", "Vector", "Matrix"])
  Existing scalar code goes under "Scalar" tab
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Vector and Matrix tabs to 9_Sensor_Ingest.py</name>
  <files>app/pages/9_Sensor_Ingest.py</files>
  <action>
Refactor 9_Sensor_Ingest.py to use st.tabs. Steps:

1. Add imports at top:
   ingest_sensor_vector, ingest_sensor_matrix, list_binning_axes_lookup

2. Load binning axes lookup alongside existing lookups:
   axes_lookup = list_binning_axes_lookup()

3. Wrap existing scalar form in tab:
   tab_scalar, tab_vector, tab_matrix = st.tabs(["Scalar", "Vector", "Matrix"])
   with tab_scalar:
       [existing scalar form code, unchanged]

4. Implement Vector tab (with tab_vector:):
   st.subheader("Vector / Spectral Sensor Ingest")
   st.markdown("Each row in the CSV is one observation (one spectrum or distribution).
   Columns: timestamp, then one column per bin in order of BinIndex.")

   Channel identity section (same pattern as scalar):
     Equipment, Parameter, Unit dropdowns
     Data Provenance, Processing Degree dropdowns
     Binning Axis: st.selectbox("Spectral / Distribution Axis",
       options=[f"{a['name']} ({a['number_of_bins']} bins)" for a in axes_lookup])
     → resolve axis_id from selection

   Show format hint when axis is selected:
     st.info(f"CSV format: timestamp, bin_0, bin_1, ..., bin_{n_bins-1}
     ({n_bins} value columns). First row may be a header.")

   Input: st.text_area + st.file_uploader (same radio pattern as scalar)

   Parsing (in parse_vector_csv(raw_text, expected_bins) → (observations, errors)):
     - Strip header row if present (detect by first cell not parseable as datetime)
     - For each data row:
       col0 = datetime.fromisoformat(row[0])
       values = [float(x) if x.strip() else None for x in row[1:]]
       If len(values) < expected_bins: pad with None
       If len(values) > expected_bins: truncate, add warning
       observations.append({timestamp: col0, bin_values: values, quality_code: None})
     Return (observations, error_messages)

   Preview: show first 5 observations as DataFrame where columns are BinIndex values
     preview_df columns: ["timestamp"] + [f"bin_{i}" for i in range(min(n_bins, 10))]
     st.caption(f"Showing first 5 observations and first 10 bins of {n_bins}")

   Submit button → ingest_sensor_vector(payload) → show channel_id + rows_written

5. Implement Matrix tab (with tab_matrix:):
   st.subheader("Matrix / 2D Distribution Sensor Ingest")
   st.markdown("Each observation is one 2D matrix at one timestamp.
   CSV format: first column is timestamp, then one column per (row_bin_index, col_bin_index)
   pair in row-major order — i.e., all col values for row 0, then all col values for row 1.")

   Channel identity section: Equipment, Parameter, Unit, DataProvenance, ProcessingDegree
   Two axis selectors in columns:
     col1: Row Axis st.selectbox (axes_lookup)
     col2: Col Axis st.selectbox (axes_lookup, different key)
   → resolve row_axis_id, col_axis_id, n_row_bins, n_col_bins

   Show format hint:
     st.info(f"CSV columns: timestamp + {n_row_bins * n_col_bins} values
     (row-major: r0c0, r0c1, ..., r{r-1}c{c-1})")

   CSV input + parsing (parse_matrix_csv(raw_text, n_rows, n_cols) → (observations, errors)):
     Parse each row: timestamp + n_rows*n_cols floats
     Reshape into matrix: [[row[i*n_cols:(i+1)*n_cols] for i in range(n_rows)]]
     observation = {timestamp, matrix, quality_code: None}

   Preview: show first 3 timestamps and first 3×3 corner of matrix as DataFrame
   Submit → ingest_sensor_matrix(payload) → show result

Store parsed state per tab using separate session_state keys to avoid cross-tab conflicts:
  st.session_state["vector_parsed"] and st.session_state["matrix_parsed"]
  </action>
  <verify>
    uv run python -m py_compile app/pages/9_Sensor_Ingest.py && echo "syntax OK"
    uv run python -c "
import ast
with open('app/pages/9_Sensor_Ingest.py') as f: src = f.read()
ast.parse(src)
# Check tabs present
assert 'st.tabs' in src, 'tabs not found'
assert 'ingest_sensor_vector' in src, 'vector import missing'
assert 'ingest_sensor_matrix' in src, 'matrix import missing'
print('checks passed')
"
  </verify>
  <done>
    File syntax-checks clean.
    Three tabs present: Scalar, Vector, Matrix.
    All three submit paths present.
    parse_vector_csv and parse_matrix_csv functions implemented.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Extended sensor ingest page with Scalar / Vector / Matrix tabs.
  </what-built>
  <how-to-verify>
    Prerequisites: Create a binning axis first on the Measurement Axes page (plan 04-04),
    e.g., "UV-Vis 200-700nm 2nm" with 250 bins.

    Scalar tab (regression):
    1. Paste a 3-row scalar CSV — verify submit still works

    Vector tab:
    2. Select the UV-Vis axis
    3. Verify format hint shows "250 value columns"
    4. Generate a test CSV with 3 rows:
         2024-01-15T10:00:00,0.01,0.02,... (250 values)
       (tip: can use Python to generate a test file)
    5. Upload the CSV — verify preview shows first 5 obs × first 10 bins
    6. Submit — verify success with channel_id and rows_written

    Matrix tab:
    7. Select two axes (row and col)
    8. Verify format hint shows correct total column count
    9. Submit a minimal test (1 observation with small matrix)
    10. Verify success response
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] st.tabs wraps all three ingest modes
- [ ] Scalar tab: existing functionality unchanged
- [ ] Vector tab: axis selector, CSV parse, preview, submit
- [ ] Matrix tab: two axis selectors, CSV parse, preview, submit
- [ ] parse_vector_csv handles header row detection + padding/truncation
- [ ] Human checkpoint approved
</verification>

<success_criteria>
- All tasks completed
- All three ingest modes functional
- CSV format hints are accurate and helpful
- No regression in scalar ingest
</success_criteria>

<output>
After completion, create .planning/phases/04-business-forms/04-08-SUMMARY.md with frontmatter:

---
phase: 04-business-forms
plan: 08
subsystem: ui
tags: [streamlit, ingest, vector, matrix, tabs]
requires:
  - phase: 04-business-forms
    provides: POST /ingest/sensor-vector, POST /ingest/sensor-matrix (04-07)
  - phase: 04-business-forms
    provides: list_binning_axes_lookup (04-04)
  - phase: 04-business-forms
    provides: scalar sensor ingest page (04-05)
provides:
  - Vector sensor ingest UI
  - Matrix sensor ingest UI
affects: []
key-files:
  created: []
  modified:
    - app/pages/9_Sensor_Ingest.py
---
</output>
