# API Reference

The open_datEAUbase API is a FastAPI application. This reference is generated automatically from the same source code that runs the server, so it always matches the deployed API.

The interactive docs below let you browse every endpoint, request schema, and response. For a hands-on guide to authenticating and making your first request, see [Authenticate with the API](../../how-to/authenticate_with_the_api.md).

<swagger-ui src="openapi.json"/>
