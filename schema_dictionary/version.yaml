schema_version: "2.5.0"
description: >
  PRD-2 S1 — Rename EquipmentEvent to Event with 8-FK exclusive arc (CHECK
  exactly-one non-NULL: Channel, Equipment, SignalInterface, DAS, SamplingPoint,
  ProcessUnit, Site, Campaign). Rename EquipmentEventKind to EventKind; expand
  seed vocab (Calibration/Cleaning/Repair/…15 kinds). Annotation FK repointed
  to Event. Pre-release, fresh install only — no migration.
date: "2026-06-30"
