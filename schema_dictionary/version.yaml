schema_version: "2.1.0"
description: >
  Consistency hardening. Drops the denormalised Channel.SignalInterfacePort_ID
  column (F3): the current port is now resolved from the active
  ChannelPortHistory row via the new vw_ChannelResolved view, giving a single
  source of truth that cannot drift. Pre-release, fresh install only — no
  migration provided.
date: "2026-06-29"
