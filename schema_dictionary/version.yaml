schema_version: "2.3.0"
description: >
  Consistency hardening part 3 — broken-link visibility (Batch 3). F6:
  vw_ChannelLocationAtTime gains Resolution + LocationResolution discriminators
  (broken-chain NULL vs genuinely-absent). F5: vw_UnlinkedChannels (raw channels
  with observations but no active wiring). F11: vw_InactiveParentReferences
  (active wiring pointing at soft-deleted interfaces/ports). Pre-release, fresh
  install only — no migration.
date: "2026-06-29"
