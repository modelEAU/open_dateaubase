_format_version: "1.0"
table:
  name: Value
  schema: dbo
  description: "Stores each measured water quality or quantity value, its time stamp, replicate identification, and the link to its specific metadata set"

  columns:
    - name: Channel_ID
      logical_type: integer
      nullable: true
      description: "The measurement channel this value belongs to"
      foreign_key:
        table: Channel
        column: Channel_ID
    - name: Value_ID
      logical_type: integer
      nullable: false
      identity: true
      description: "A unique ID is generated automatically by the database"
    - name: Value
      logical_type: float64
      nullable: true
      description: "Value of collected data"
    - name: Timestamp
      logical_type: timestamp
      precision: 7
      nullable: true
      description: "UTC timestamp for date and time of collected data (stored in UTC by convention)"

  primary_key: [Value_ID]
