DB_HOST=db
DB_PORT=1433
DB_NAME=open_dateaubase
DB_USER=SA
DB_PASSWORD=StrongPwd123!

# Shared service token for machine clients (e.g. dev auto-login and the importer).
# Set this to the same value as the API's API_SERVICE_TOKEN. In production this
# must be a strong, externally-managed secret.
API_SERVICE_TOKEN=dev-service-token

